(ns cards_project.cards.card-ability-queries
  (:require [hugsql.core :as hugsql]
            [cards_project.db]))   ; loads set-adapter! side effect

(hugsql/def-db-fns "sql/cards/card_ability.sql")
